# 体考体测落地检查清单（对照总设计文档）

更新时间：2026-04-04

## 已完成

1. 双业务板块已分离：`PE` 与 `Fitness` 前后端路由和页面均已独立。
2. 前端图表/列表页面已从静态数据切换为后端接口驱动。
3. 体考补齐“成绩录入”页面：
   - `kinit-admin/src/views/Vadmin/PE/Entry/Entry.vue`
   - 路由 `PEEntry` 已加入。
4. 后端体考/体测接口均已提供：
   - 总览、学生分析、班级分析、年级分析、录入模板、报表配置、标准列表。
5. 角色权限控制已落在接口层：
   - 体考 `pe.*` 权限码
   - 体测 `fitness.*` 权限码
   - 使用 `FullAdminAuth(permissions=[...])` 控制。
6. 数据权限基础处理已加入（学校/班级维度）：
   - 按 `data_range/dept_ids` 做示例级过滤逻辑。
7. 新增闭环业务 SQL：
   - `docs/db/20260404_pe_fitness_closed_loop.sql`
   - 覆盖标准主表、标准项目、批次、成绩明细。
8. 新增统一评分引擎基础文件：
   - `kinit-api/apps/vadmin/sport/service/rule_engine.py`
9. 编码规范问题（BOM/乱码风险）已处理过新增关键文件。

## 待补强（下一轮建议）

1. 评分引擎接入真实数据流：
   - 当前 `rule_engine.py` 已有基础实现，但尚未接入录入保存流程。
2. 真正持久化闭环：
   - 当前统计接口仍以样例数据返回，尚未读取 `vadmin_pef_*` 表实时聚合。
3. 学生角色前端入口：
   - 后端已加 `analysis/student/self` 接口；前端尚未增加学生视角独立路由与菜单绑定。
4. 标准导入链路：
   - PDF/Excel/手工录入的解析与人工确认流程尚未落数据库流程。
5. 报表导出：
   - 当前为配置与界面联动，未生成真实文件导出。

## 风险提示

1. 若直接切到真实数据库聚合，需先执行新增 SQL 并准备样本数据。
2. 现有系统 TypeScript 全量检查本身存在历史问题，需独立一轮工程化清理。

## 本轮补充（2026-04-04）

1. PE/Fitness 的 `overview`、`analysis/student`、`analysis/class`、`analysis/grade` 已改为数据库聚合（基于 `vadmin_pef_batch` + `vadmin_pef_score`），不再依赖页面样例数据。
2. `batch/options`、`batch`、`score/upsert` 已补充学校/班级维度的数据权限限制（结合 `data_range/dept` 过滤逻辑）。
3. 新增统一聚合工具：`kinit-api/apps/vadmin/sport/service/analytics_service.py`，用于批次查询、成绩查询、分组聚合与比率计算。
4. 已补充查询参数 `school_id/grade_id/class_id` 到名称过滤映射（通过 `vadmin_auth_dept` 解析），前端传 ID 也可生效。
5. `score/upsert` 已接入“仅录入成绩自动计算分值/状态”：按批次绑定标准 + 标准项 + 性别选择规则，自动回填 `score_value/is_pass/is_excellent/is_full`（可与手工值混用）。
